# 一次性替代镜像：跳过 pnpm install + pnpm build（npmmirror 在该环境超时），
# 仅把宿主机全新构建的 dist/ 与最新 nginx.conf 注入 nginx 运行阶段。
FROM nginx:1.27-alpine
# 直接使用宿主机最新 nginx.conf（含 index.html 不缓存配置），确保缓存策略生效
COPY web/nginx.conf /etc/nginx/conf.d/default.conf
COPY web/dist /usr/share/nginx/html

EXPOSE 80