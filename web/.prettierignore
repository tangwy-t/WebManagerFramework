/node_modules/*
/dist/*
/src/main.ts
# 生成物:由 server/tools/apigen 从 Go 响应 DTO 生成(见 pnpm gen:api)。
# 它必须与生成器输出**逐字节一致**,否则 check:api-types 的
# "重新生成并 git diff --exit-code" 会永远失败。
# prettier 会把生成器的缩进规整掉 → 提前排除,而非每次生成后再格式化。
/src/types/api/api.generated.d.ts
# 生成物:由 scripts/offline-icons.mjs 生成(见 pnpm icons:sync)。
# `pnpm check:icons` 会拿"重新生成一遍"的结果与磁盘文件做**逐字节**比对,
# 用来抓"手改生成物"与"依赖里的图标数据升级后内容过期"这两类名字对得上、
# 内容却不同的漂移。prettier 一改就会让这个比对永远失败 —— 与
# api.generated.d.ts 同款处理:提前排除,而不是每次生成后再格式化。
/src/utils/ui/offline-icons.generated.ts