node_modules
# pnpm 沙箱内本地内容存储(sandbox 模式下 pnpm 的全局 store 不可写,重定向到工作区内)
.pnpm-store
.DS_Store
dist
dist-ssr
*.local
.cursorrules
tsc-errors.log
# IDE 配置
.idea/

# Auto-generated (eslint globals only)
.auto-import.json

# 本机 pnpm 配置(内含 store-dir 指向本机绝对路径,不入库)
.npmrc

# 环境变量(只提交 .env.example 模板)
.env
.env.*
!.env.example